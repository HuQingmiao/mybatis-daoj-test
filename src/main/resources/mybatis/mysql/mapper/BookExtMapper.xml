<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC  "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="walker.mybatisdaoj.test.dao.BookDao">

    <select id="findBooks" resultType="walker.mybatisdaoj.test.dpo.Book">
        SELECT book_id,title,price,publish_time
        FROM book
        <where>
            <if test="bookId!= null">
                AND book_id = #{bookId}
            </if>
            <if test="title!= null">
                AND title like #{title}
            </if>
            <if test="minPrice!= null">
                <![CDATA[  AND  price >= #{minPrice}  ]]>
            </if>
            <if test="maxPrice!= null">
                <![CDATA[  AND  price < #{maxPrice}   ]]>
            </if>
            <if test="publishTime!= null">
                AND publish_time = #{publishTime}
            </if>
        </where>
    </select>

    <select id="findByPrice" resultType="walker.mybatisdaoj.test.dpo.Book">
        SELECT book_id,title,price,publish_time
        FROM book
        <where>
            <if test="minPrice!= null">
                <![CDATA[  AND  price >= #{minPrice}  ]]>
            </if>
            <if test="maxPrice!= null">
                <![CDATA[  AND  price < #{maxPrice}   ]]>
            </if>
        </where>
    </select>


</mapper>

